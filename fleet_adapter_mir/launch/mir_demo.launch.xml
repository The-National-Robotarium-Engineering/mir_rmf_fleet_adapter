<?xml version='1.0' ?>

<launch>
  <arg name="failover_mode" default="false"/>
  <arg name="sim_update_rate" default='100'/>
  <arg name="use_sim_time" default="false" description="Use the /clock topic for time to sync with simulation"/>
  <arg name="viz_config_file" default="$(find-pkg-share rmf_visualization_schedule)/config/rmf.rviz"/>
  <arg name="building_file" default="$(find-pkg-share fleet_adapter_mir)/configs/raslab_03_09_2025.building.yaml" description="Building description file required by rmf_building_map_tools"/>
  <arg name="config_file" default="$(find-pkg-share fleet_adapter_mir)/configs/mir_config.yaml" description="Config file required by fleet_adapter_mir"/>
  <arg name="nav_graph_file" default="$(find-pkg-share fleet_adapter_mir)/configs/0.yaml" description="Nav Graph file required by fleet_adapter_mir"/>
  <arg name="mission_file" default="$(find-pkg-share fleet_adapter_mir)/missions/rmf_missions.json" description="Mission file required by fleet_adapter_mir for creating rmf_missons in MiR"/>
  <arg name="initial_map" default="L1" description="Initial map name for the visualizer"/>
  <arg name="headless" default="false" description="do not launch rviz; launch gazebo in headless mode"/>
  <arg name="bidding_time_window" description="Time window in seconds for task bidding process" default="2.0"/>
  <arg name="use_unique_hex_string_with_task_id" default="true" description="Appends a unique hex string to the task ID"/>
  <arg name="server_uri" default="192.168.2.101" description="The URI of the api server to transmit state and task information."/>
  <arg name="use_reservation_node" default="false" description="Enable the reservation node for parking spot de-confliction"/>
  <arg name="output" default="screen"/>
  <!-- Traffic Schedule  -->
  <node pkg="rmf_traffic_ros2" exec="rmf_traffic_schedule" output="both" name="rmf_traffic_schedule_primary">
    <param name="use_sim_time" value="$(var use_sim_time)"/>
  </node>

  <!-- Blockade Moderator -->
  <node pkg="rmf_traffic_ros2" exec="rmf_traffic_blockade" output="both">
    <param name="use_sim_time" value="$(var use_sim_time)"/>
  </node>

  <!-- Building Map  -->
  <group>
    <node pkg="rmf_building_map_tools" exec="building_map_server" args="$(var building_file)">
      <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>
  </group>

  <!-- Visualizer -->
  <group>
    <include file="$(find-pkg-share rmf_visualization)/visualization.launch.xml">
      <arg name="use_sim_time" value="$(var use_sim_time)"/>
      <arg name="map_name" value="$(var initial_map)"/>
      <arg name="viz_config_file" value ="$(var viz_config_file)"/>
      <arg name="headless" value="$(var headless)"/>
    </include>
  </group>

  <!-- Door Supervisor -->
  <group>
    <node pkg="rmf_fleet_adapter" exec="door_supervisor">
      <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>
  </group>

  <!-- Lift Supervisor -->
  <group>
    <node pkg="rmf_fleet_adapter" exec="lift_supervisor">
      <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>
  </group>

  <!-- Dispatcher Node -->
  <group>
    <node pkg="rmf_task_ros2" exec="rmf_task_dispatcher"  output="screen">
      <param name="use_sim_time" value="$(var use_sim_time)"/>
      <param name="bidding_time_window" value="$(var bidding_time_window)"/>
      <param name="use_unique_hex_string_with_task_id" value="$(var use_unique_hex_string_with_task_id)"/>
      <param name="server_uri" value="$(var server_uri)"/>
    </node>
  </group>

  <!-- Proto-Reservation system-->
  <group  if="$(var use_reservation_node)">
    <node pkg="rmf_reservation_node" exec="queue_manager"/>
  </group>

  <!-- Mutex Group Supervisor, manages the locking and unlocking of mutex groups -->
  <group>
    <node pkg="rmf_fleet_adapter" exec="mutex_group_supervisor"/>
  </group>

  <!-- Fleet Adapter and Robot State Aggregator for MIR -->
  <group>
    <let name="fleet_name" value="mir_raslab"/>
    <include file="$(find-pkg-share fleet_adapter_mir)/launch/mir_adapter.launch.xml">
      <arg name="fleet_name" value="$(var fleet_name)"/>
      <arg name="use_sim_time" value="$(var use_sim_time)"/>
      <arg name="nav_graph_file" value="$(var nav_graph_file)" />
      <arg name="config_file" value="$(var config_file)" />
      <arg name="mission_file" value="$(var mission_file)" />
    </include>
    <include file="$(find-pkg-share rmf_fleet_adapter)/robot_state_aggregator.launch.xml">
      <arg name="robot_prefix" value="mir"/>
      <arg name="fleet_name" value="$(var fleet_name)"/>
      <arg name="use_sim_time" value="true"/>
    </include>
  </group>


</launch>