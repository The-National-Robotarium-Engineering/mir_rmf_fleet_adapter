<?xml version='1.0' ?>

<launch>
    <arg name="use_sim_time" default="false" description="Use the /clock topic for time to sync with simulation"/>
    <arg name="config_file" description="The config file that provides important parameters for setting up the adapter"/>
    <arg name="nav_graph_file" description="The graph that this fleet should use for navigation"/>
    <arg name="mission_file" description="The mission file that this fleet should use for creating RMF missions in MiR"/>
    <arg name="output" default="screen"/>

    <!-- Fleet adapter -->
    <node pkg="fleet_adapter_mir"
        exec="fleet_adapter_mir"
        args="-c $(var config_file) -n $(var nav_graph_file) -r $(var mission_file)"
        name="fleet_adapter_mir"
        output="screen">
        <param name="use_sim_time" value="$(var use_sim_time)"/>
    </node>
</launch>
